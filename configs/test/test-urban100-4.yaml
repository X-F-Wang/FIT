#
test_dataset:
  dataset:
    name: image-folder
    args:
      root_path: /home/data/Urban100/HR
  wrapper:
    name: sr-implicit-downsampled-cascaded
    args:
      batch_size: 1
      scale_max: 4
      scale_base: 4
      phase: valid
  batch_size: 1

eval_type: benchmark-4
eval_bsize: 30000

data_norm:
  inp: {sub: [0.5], div: [0.5]}
  gt: {sub: [0.5], div: [0.5]}